import React from 'react';
import { TouchableOpacity, View } from 'react-native';
import Globals from '../Globals';
import AddSVG from './SVGComponentes/addSVG';
type props = {
    navigation: any;
}
export default function BotaoAdd({ navigation }: props["navigation"]) {

    return (
     
    );
}
